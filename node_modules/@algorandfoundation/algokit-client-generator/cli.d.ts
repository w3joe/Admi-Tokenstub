import { GeneratorOptions } from './client/generator-context';
export declare function cli(workingDirectory: string, args: string[]): void;
export declare function generateClientCommand({ application, output, preserveNames, mode, workingDirectory, }: {
    application: string;
    output: string;
    workingDirectory: string;
} & GeneratorOptions): Promise<void>;
