{
  "scripts": {
    "bootstrap": "algokit project bootstrap all",

    "deps:frontend:needed": "bash -lc 'DIR=projects/QuickStartTemplate-frontend; [ -d \"$DIR/node_modules\" ] || (npm -C \"$DIR\" ci || npm -C \"$DIR\" install)'",
    "deps:backend:needed": "bash -lc 'DIR=projects/QuickStartTemplate-contracts; [ -d \"$DIR/node_modules\" ] || (npm -C \"$DIR\" ci || npm -C \"$DIR\" install)'",
    "deps:needed": "npm run deps:frontend:needed && npm run deps:backend:needed",

    "setup": "npm run deps:needed && npm run bootstrap",

    "dev": "npx -y concurrently -k -n FRONTEND,SERVER -c auto \"cd projects/QuickStartTemplate-frontend && npm run dev\" \"cd projects/QuickStartTemplate-contracts && node nft_mint_server/server.js\""
  }
}
